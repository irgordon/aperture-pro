(function () {
    const root = document.getElementById('aperture-pro-app');
    if (!root) return;

    // Example: simple route awareness, could be extended later
    console.log('Aperture Pro SPA theme loaded');
})();
